import React from 'react'

const SearchBar = () => {
    return (
        <div>
            <input type="text" />
            <div>
                <input type="checkbox" />
                <span>Only show products in stock</span>
            </div>
        </div>
    )
}

export default SearchBar;
